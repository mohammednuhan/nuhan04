# Base URL for the portfolio backend API.
# In development, Vite proxies /api to http://localhost:5000, so you can leave this unset.
# In production, set this to your deployed backend URL (e.g. https://your-api.example.com/api).
VITE_API_URL=/api
